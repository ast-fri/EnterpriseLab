version: '3.8'

services:
  email-mcp:
    build: .
    container_name: email-mcp-server
    ports:
      - "12005:12005"
    environment:
      - EMAIL_BASE_URL=http://host.docker.internal:3002

    restart: unless-stopped